## Mermaid follows the palette

```mermaid
graph LR
  A[Config] --> B(theme: editorial)
  A --> C(mode: light)
  B & C --> D{Rendered deck}
```

Nodes blend with the slide background, edges use the muted color.
